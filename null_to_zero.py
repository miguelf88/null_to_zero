#defining the function
def updateValue(value):
  
	#checks to see if there is a value in the field
	if value == None:
   
  		#if there is no value or <null>, it returns a 0
  		return '0'
  
	#if there is a value, it returns that value
	else: 

		return value